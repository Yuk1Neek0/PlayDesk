---
name: Signature helper coordination (extract or import the shared verifier)
status: closed
created: 2026-05-24T01:00:22Z
updated: 2026-05-24T02:03:46Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/149
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Signature helper coordination (extract or import the shared verifier)

## Description

This slice and the parallel `whatsapp` slice both need a shared Twilio signature-verification helper. Coordinate cleanly: if `backend/agent/channels/twilio_signature.py` already exists (whatsapp slice landed first), just import it. Otherwise, extract it from the existing `twilio_sms.py` inline verifier — same shape as whatsapp slice's task 001.

## Acceptance Criteria
- [ ] `backend/agent/channels/twilio_signature.py` exists and exports `verify_twilio_signature(url, post_data, signature, auth_token) -> bool`.
- [ ] If the file existed before this task (whatsapp slice landed first), no changes made — just a noop task closing the dep gap.
- [ ] If the file didn't exist, this task extracts it from `twilio_sms.py` AND refactors the SMS adapter + webhook to import from there. Pure refactor — no behaviour change.
- [ ] Existing 397-test suite passes unchanged regardless of which slice extracted the helper.
- [ ] One unit test in `tests/test_twilio_signature.py` covering signed-OK / tampered-bad / empty-token-bad (if not already present from whatsapp slice).

## Technical Details

- This task is intentionally idempotent across both v5 slices. The first one to merge does the real work; the second imports.
- When this slice runs against `main` AFTER whatsapp slice has merged: this task is a 5-minute "verify the import works" check.
- When this slice runs BEFORE whatsapp slice merges: this task does the full extraction.

## Dependencies

- None. First task in this slice.

## Definition of Done
- [ ] Code implemented (or verified to already exist)
- [ ] Tests written and passing
- [ ] Code reviewed
