---
name: Admin frontend — /admin/customers + customer detail page
status: closed
created: 2026-82-23T04:27:55Z
updated: 2026-05-23T14:10:41Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/83
depends_on: [82]
parallel: true
conflicts_with: []
---

# Task: Admin frontend — /admin/customers + customer detail page

## Description

Two new staff-gated routes: a customers list with debounced search and pagination, and a detail page showing profile + visit history + notes log with an "add note" form.

## Acceptance Criteria

- [ ] `/admin/customers` lists customers with columns `Name | Phone | Last visit | Total visits | Tags`; search debounced 300ms.
- [ ] Row click navigates to `/admin/customers/[id]`.
- [ ] Detail page renders profile card + visit history table (datetime, resource, status, source) + notes log + "add note" form.
- [ ] Auth gate redirects non-staff to `/login` (existing pattern).
- [ ] Reuses `pd-*` styles — no new CSS file.

## Technical Details

- `frontend/src/app/admin/customers/page.tsx` + `frontend/src/app/admin/customers/[id]/page.tsx`.
- New client functions in `frontend/src/lib/api.ts`: `listCustomers(q, page)`, `getCustomer(id)`, `addCustomerNote(id, body)`.

## Dependencies

- 82 — endpoints.

## Effort Estimate
- Size: M
- Hours: 6

## Definition of Done
- [ ] Both pages render against live stack
- [ ] Auth gate verified
- [ ] Code reviewed
