---
name: Migration — Customer + CustomerNote + Booking.customer FK
status: closed
created: 2026-82-23T04:27:55Z
updated: 2026-05-23T14:10:41Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/78
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Migration — Customer + CustomerNote + Booking.customer FK

## Description

Single Django migration introducing the `Customer` and `CustomerNote` models, the `Booking.customer` FK (nullable for now), the unique constraint on `(store_id, phone)`, and supporting indexes.

## Acceptance Criteria

- [ ] `core_customer` table exists with columns: `id, store_id (FK), phone (varchar 32), name, email (nullable), locale_pref (varchar 2 default 'en'), created_at, last_visit_at (nullable), total_visits (int default 0), tags (jsonb default []).
- [ ] Unique constraint on `(store_id, phone)`.
- [ ] GIN index on `lower(name)` for fuzzy search.
- [ ] `core_customernote` table exists: `id, customer_id (FK), author_id (FK to auth_user, nullable), body, created_at`.
- [ ] `core_booking.customer_id` column added (nullable, FK to Customer, on_delete=PROTECT).
- [ ] Forward and reverse migrations apply against a populated DB.
- [ ] Existing 157 tests still pass (FK is nullable so no fixture breaks).

## Technical Details

- All FKs use `on_delete=PROTECT` for Customer (no accidental delete of someone with bookings).
- Tags is a `JSONField(default=list)`.

## Dependencies

None.

## Effort Estimate
- Size: S
- Hours: 3

## Definition of Done
- [ ] Migration in + reversible
- [ ] Existing suite green
- [ ] Code reviewed
