---
name: Agent _build_customer_context cohort line + retention.e2e.ts
status: open
created: 2026-05-24T15:30:00Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/213
depends_on: [212]
parallel: false
conflicts_with: []
---

# Task: Agent prompt extension + e2e

## Description

Two small things: extend v11b's `_build_customer_context` in `agent/loop.py` to include the cohort, and a Playwright e2e covering the filter + bulk-send + cohort visibility.

## Acceptance Criteria

- [ ] `backend/agent/loop.py::_build_customer_context`:
   - Add `lines.append(f"- Cohort: {customer.cohort}")` after the existing `Total visits:` line, only if `customer.cohort != "new"` (avoid noise for brand-new customers — the agent already knows from the visit count).
   - Unit test in `backend/agent/test_customer_context.py`: a customer with `cohort="at_risk"` produces a context block containing "Cohort: at_risk".
- [ ] `frontend/e2e/retention.e2e.ts`:
   - **Test 1 — sweeper runs**: invoke the management command via a test-only endpoint OR via Playwright's beforeAll calling `python manage.py recompute_retention`. After the run, navigate to `/admin/customers/` and assert the cohort counts chip is populated.
   - **Test 2 — filter by cohort**: click "Dormant" in the filter → list shows only dormant customers.
   - **Test 3 — bulk send**: with the Dormant filter active, click "Send re-engagement to all N visible" → confirm modal → submit → toast "Sent to N, skipped M".
   - **Test 4 — opt-out respected**: a customer with `sms_opt_out` tag in the dormant cohort doesn't receive the message (verify by checking the outbound queue / mock).
- [ ] Existing 739-test backend suite + 108 frontend suite pass.
- [ ] `npm run lint && npm run typecheck && npm test && npm run build` clean.

## Technical Details

- The agent context line addition is genuinely a 1-line change. Trivial review.
- The e2e is the bigger chunk. Test setup needs:
   - Seed customers across cohorts (extend seed_data.py): 3 active, 2 at_risk, 2 dormant, 1 lost, 1 dormant-with-sms_opt_out.
   - Run `recompute_retention` either via test-only endpoint or via Playwright global setup.
- For the bulk-send assertion, the simplest check is to count `OutboundMessage` rows created with the `re_engagement_60d` template_key (or the queue table — whatever v4 outbound uses).

## Dependencies

- Task 212: filter + bulk-send shipped.

## Definition of Done

- [ ] Agent prompt extension + unit test
- [ ] e2e test passes locally
- [ ] Backend + frontend suites green
- [ ] `npm run lint && npm run typecheck && npm test && npm run build` clean
