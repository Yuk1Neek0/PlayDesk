---
name: Migration — QRAction + QREvent + Store.slug + Store.brand
status: closed
created: 2026-89-23T04:27:55Z
updated: 2026-05-23T14:10:41Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/85
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Migration — QRAction + QREvent + Store.slug + Store.brand

## Description

Single Django migration adding the QR module's schema plus the supporting `Store` fields. Backfills `Store.slug` from existing names.

## Acceptance Criteria

- [ ] `core_qraction`: `id, store_id (FK), kind (varchar), label, target_url, position (int), reward_points (int default 0), enabled (bool default true)`. Unique on `(store_id, position)`.
- [ ] `core_qrevent`: `id, action_id (FK nullable), customer_id (FK nullable), kind (varchar: scan|click), created_at, user_agent (varchar 255), locale (varchar 2 nullable)`.
- [ ] `core_store.slug (varchar 64 unique)` + `core_store.brand (jsonb default {})`. Slug backfilled from `slugify(name)` with collision suffix `-2`, `-3`, ... per store.
- [ ] Forward + reverse migrations apply cleanly.
- [ ] Existing 157-test suite still passes.

## Technical Details

- Slug is stable: never auto-regenerated after first save. A separate admin action (out of scope here) renames a store's slug deliberately.

## Dependencies

None.

## Effort Estimate
- Size: S
- Hours: 3

## Definition of Done
- [ ] Migration + tests
- [ ] Code reviewed
