---
name: SSE streaming endpoint
status: closed
created: 2026-05-21T19:51:40Z
updated: 2026-05-21T20:48:16Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/14
depends_on: [13]
parallel: false
conflicts_with: [13, 15]
---

# Task: SSE streaming endpoint

## Description

Expose the agent loop over an SSE streaming endpoint in the `backend/agent/` app.

## Acceptance Criteria
- [ ] `POST /api/conversations/{id}/messages/` accepts a user message and streams the assistant response.
- [ ] Conversation creation endpoint (or auto-create) so a client can start a conversation.
- [ ] SSE events match `docs/contracts/sse-protocol.md`: `token`, `tool_call_start`, `tool_call_end`, `done`, `error`.
- [ ] Tokens are emitted incrementally, not as a single payload.
- [ ] The user message and final assistant message are persisted.
- [ ] pytest asserts the event sequence with a mocked streaming LLM.

## Technical Details
- Django `StreamingHttpResponse` with `text/event-stream`.
- Code under `backend/agent/`; report the `config/urls.py` include needed.
- Wraps the task 004 loop — emits its tokens/tool events as SSE.

## Dependencies
Task 004 (agent loop). Same execution stream.

## Effort Estimate
- Size: M
- Hours: 4

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
