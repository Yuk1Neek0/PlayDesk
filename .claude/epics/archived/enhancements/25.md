---
name: Stripe sandbox deposits
status: open
created: 2026-05-21T23:41:41Z
updated: 2026-05-21T23:45:48Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/25
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Stripe sandbox deposits

## Description

Add Stripe Checkout (test mode) deposits to the booking flow. On
`create_booking` a Checkout session is created; a webhook flips the booking
from `pending_payment` to `confirmed`; a periodic command expires stale
`pending_payment` holds.

## Acceptance Criteria
- [ ] `create_booking` produces a booking in `pending_payment` with a Stripe Checkout (test mode) session.
- [ ] A webhook endpoint flips `pending_payment` → `confirmed` on successful payment.
- [ ] A management command expires `pending_payment` bookings older than the hold window.
- [ ] Stripe calls are mocked in tests — runs in CI without secrets.
- [ ] CI green: `ruff check`, `ruff format`, `pytest`.

## Technical Details

- No schema change — `Booking.status = pending_payment` already exists from foundation.
- Stripe in test mode only; no production hardening.
- Disjoint code path — conflict-free with tasks 001, 003, 004.

## Dependencies

- `backend-core` epic (booking flow, agent tools) — merged.

## Effort Estimate
- Size: M
- Hours: 4

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
