---
name: "Stream C: frontend integration verification"
status: open
created: 2026-05-22T12:27:10Z
updated: 2026-05-22T12:35:59Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/34
depends_on: [31]
parallel: true
conflicts_with: []
---

# Task: Stream C — frontend integration verification

## Description

Verify the dev plan's frontend acceptance criteria (§1.2) against the live backend — the work the `frontend` epic explicitly deferred from mocks to "Wave 2". Fix any contract-drift bug found, in-stream.

## Acceptance Criteria
- [ ] `/` manual booking flow (resource → date → time → confirm) completes against the live REST API and the booking persists.
- [ ] `/chat` streams real tokens from the live SSE endpoint; "checking availability…" / "looking up policy…" tool-call hints render while a tool call is in flight.
- [ ] A booking completed through `/chat` appears in `/admin` without a manual refresh.
- [ ] `/admin` shows live conversations and all bookings sorted by `created_at` desc.
- [ ] The chat UI does not freeze during long tool-call sequences.
- [ ] Verification record written to `updates/004/`: criterion → pass/fail → steps + screenshot/observation.

## Technical Details

- Run the browser against the live `docker compose` backend, not mocked fetch/SSE.
- Likely fix surface: API base URL config, SSE event-name mismatch between `frontend/src/lib/{api,sse}.ts` and the backend's emitted events, or `frontend/src/types/api.d.ts` drift from `docs/contracts/openapi.yaml` (regenerate if so).
- No-refresh propagation: confirm the polling/refetch mechanism on `/admin` actually picks up the new booking.
- Primary fix surface: `frontend/`. `types/api.d.ts` regeneration is flagged for Task 006 if it affects shared generated output.

## Dependencies

- Task 001 (stack booted).

## Effort Estimate
- Size: M
- Hours: 4

## Definition of Done
- [ ] All §1.2 criteria pass against the live backend
- [ ] Contract-drift bugs fixed in-stream
- [ ] `npm run lint && typecheck && test && build` still green
- [ ] Verification record written to `updates/004/`
