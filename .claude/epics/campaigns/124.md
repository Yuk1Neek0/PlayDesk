---
name: Admin endpoints — segments CRUD + preview, campaigns CRUD + send + cancel, runs list
status: open
created: 2026-05-23T14:19:04Z
updated: 2026-05-23T14:49:31Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/124
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Admin endpoints — segments CRUD + preview, campaigns CRUD + send + cancel, runs list

## Description

The DRF surface. Standard ModelViewSets for segments and campaigns, plus three custom actions: segment-preview, campaign-send, campaign-cancel, and a runs list filtered by status.

State-machine enforcement is delegated to `send_campaign` / `cancel_campaign` from task 004; the view layer just translates exceptions to HTTP codes.

## Acceptance Criteria
- [ ] `/api/admin/segments/` ModelViewSet: list / create / retrieve / partial_update / destroy. List + create are store-scoped to the request user's accessible stores.
- [ ] `GET /api/admin/segments/{id}/preview/?limit=20` returns `{ count: int, sample: [...up to limit customers] }`. `count` is the full match count; `sample` is the first N for display.
- [ ] `/api/admin/campaigns/` ModelViewSet: list / create / retrieve / destroy. `partial_update` allowed only when `status == "draft"` — returns 409 otherwise.
- [ ] `POST /api/admin/campaigns/{id}/send/` requires `{"confirm": true}` in the body; rejects without 400; calls `send_campaign(id, sent_by=request.user)`; returns the runner's summary dict. Maps `CampaignAlreadyProcessed → 409`, `CampaignTooLarge → 400`.
- [ ] `POST /api/admin/campaigns/{id}/cancel/` calls `cancel_campaign(id)`; returns the updated campaign. 409 if past `scheduled`.
- [ ] `GET /api/admin/campaigns/{id}/runs/?status=failed&page=N&page_size=50` returns paginated `CampaignRun` rows, optionally filtered by status.
- [ ] All endpoints behind the admin auth gate; all store-scoped (cross-store reads return 404, not 403).
- [ ] Tests cover: segment CRUD round-trip + preview shape + cross-store 404; campaign CRUD; PATCH on sent → 409; send happy path → status flips to sent; second send → 409; cancel on draft → cancelled; cancel on sent → 409; send without `confirm: true` → 400; runs list pagination.

## Technical Details

- File: `backend/api/views_campaigns.py` (new) + URL conf.
- Reuse the existing admin-auth permission class.
- Pagination: use DRF's `PageNumberPagination` with `page_size_query_param="page_size"`, default 50, max 200.
- Serializers in `backend/api/serializers_campaigns.py` — keep validation rules (`body_template` non-empty, `scheduled_for` not in the past unless draft) at the serializer level.

## Dependencies

- Task 002 (segment evaluator).
- Task 004 (runner — `send_campaign` + `cancel_campaign`).

## Effort Estimate
- Size: M
- Hours: 4

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
