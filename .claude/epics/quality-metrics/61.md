---
name: Offline mode + fixture — CI-deterministic report
status: open
created: 2026-61-23T04:57:34Z
updated: 2026-05-23T04:10:23Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/61
depends_on: [60]
parallel: true
conflicts_with: []
---

# Task: Offline mode + fixture — CI-deterministic report

## Description

Add `--offline` flag to the report generator so CI can regenerate the file without hitting an LLM. Reads from a checked-in `backend/evals/fixtures/sample_messages.json`.

## Acceptance Criteria

- [ ] `python -m evals.quality_report --offline` produces an identical report on every run, without network access.
- [ ] `sample_messages.json` is committed and covers ≥2 prompt versions and ≥2 model names so the report's per-version section is meaningful.
- [ ] A new CI step runs the generator in offline mode and fails on diff drift.

## Technical Details

- `backend/evals/fixtures/sample_messages.json` — small enough to read in tests (<200KB).
- The CI step: `python -m evals.quality_report --offline && git diff --exit-code docs/reports/ai-quality-report.md`.

## Dependencies

- 60 — generator must exist.

## Effort Estimate
- Size: S
- Hours: 3

## Definition of Done
- [ ] Fixture committed
- [ ] Offline mode + CI check
- [ ] Code reviewed
