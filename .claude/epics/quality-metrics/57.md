---
name: Aggregation core — shared null-safe functions in evals/aggregates.py
status: open
created: 2026-61-23T04:57:34Z
updated: 2026-05-23T04:10:23Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/57
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Aggregation core — shared null-safe functions in evals/aggregates.py

## Description

A single module that backs both the metrics endpoint and the report generator. Functions: `ai_booking_conversion(days)`, `handoff_rate(days)`, `avg_response_time_ms(days)`, `est_revenue_assisted(days)`, `bookings_by_source(days)`, `pass_rate_by_prompt_version(eval_results)`.

## Acceptance Criteria

- [ ] All functions tolerate zero rows — return `None` (or `0` where semantically appropriate) without raising.
- [ ] All functions tolerate missing Stream A/B fields (NULL `latency_ms`, NULL `prompt_version`).
- [ ] Single aggregate query per function — no N+1.
- [ ] Six unit tests covering zero, null fields, and a populated dataset for each function.

## Technical Details

- `backend/evals/aggregates.py`.
- Pure functions; take a `days: int` window argument.
- No model or migration changes.

## Dependencies

None.

## Effort Estimate
- Size: S
- Hours: 4

## Definition of Done
- [ ] Functions + null-safety tests
- [ ] Code reviewed
