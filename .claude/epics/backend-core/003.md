---
name: 'Agent tools: real implementations of the 6 tools'
status: open
created: 2026-05-21T19:51:40Z
updated: 2026-05-21T19:51:40Z
github: (will be set on sync)
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Agent tools — real implementations

## Description

Replace the foundation stub bodies in `backend/agent_tools/` with real implementations, wired to the database and the RAG layer. Pydantic schemas and the registry are frozen — only bodies change.

## Acceptance Criteria
- [ ] `search_knowledge_base` — calls the `rag` retriever (task 002).
- [ ] `check_availability` — computes real open slots; returns `available` and `suggestions` (suggestions may stay empty here — populated by the enhancements epic).
- [ ] `get_resource_details` — real specs/pricing/equipment from `Resource`/`GameMenu`.
- [ ] `create_booking` — creates a `Booking`; returns `booking_id` or a structured conflict error on overlap.
- [ ] `modify_booking`, `cancel_booking` — adjust/cancel real bookings.
- [ ] Tool failures return structured error objects, never raise.
- [ ] pytest covers each tool against a test DB; the registry/schema tests still pass.

## Technical Details
- Only edit files under `backend/agent_tools/`; import the retriever from `rag`.
- Reuse availability/booking logic patterns; do not duplicate the `EXCLUDE`-based conflict handling.

## Dependencies
Task 002 (RAG retriever) for `search_knowledge_base`. Same execution stream as 002.

## Effort Estimate
- Size: M
- Hours: 4

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
