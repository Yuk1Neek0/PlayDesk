---
name: ChannelAdapter ABC + WebChatAdapter rewrite
status: open
created: 2026-96-23T04:27:55Z
updated: 2026-05-23T04:34:25Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/93
depends_on: [92]
parallel: false
conflicts_with: []
---

# Task: ChannelAdapter ABC + WebChatAdapter rewrite

## Description

New `backend/agent/channels/` package with a pure-Python `ChannelAdapter` ABC (`normalize_inbound`, `format_outbound`) and `WebChatAdapter` as the first concrete adapter. The existing `POST /api/conversations/{id}/messages/` view is rewritten to route through it. The SSE protocol shape MUST stay byte-identical so the Playwright suite passes unchanged.

## Acceptance Criteria

- [ ] `ChannelAdapter` ABC in `backend/agent/channels/base.py` with no Django imports.
- [ ] `NormalizedMessage` dataclass: `text, customer_identifier, channel, language, raw_payload`.
- [ ] `WebChatAdapter` implements both methods; the existing view delegates to it.
- [ ] Existing chat e2e tests pass unchanged.
- [ ] One new unit test exercises `WebChatAdapter` in isolation (no DRF).

## Technical Details

- `agent/channels/base.py` and `agent/channels/web_chat.py`.
- Keep the inline view code thin — the view should be ~15 LOC after the rewrite.

## Dependencies

- 92 — `Conversation.channel` must exist so the adapter can set it correctly.

## Effort Estimate
- Size: M
- Hours: 6

## Definition of Done
- [ ] ABC + WebChat adapter
- [ ] Existing tests pass
- [ ] Code reviewed
