---
name: Combined migration — Conversation.status + handoff fields + PromptConfig + seed
status: open
created: 2026-55-23T04:51:34Z
updated: 2026-05-23T04:10:12Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/51
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Combined migration — Conversation.status + handoff fields + PromptConfig + seed

## Description

One Django migration that adds:
- `Conversation.status` (choices: `active | ai_resolved | needs_human | staff_takeover | closed`, default `active`)
- `Conversation.handoff_reason` (text, nullable)
- `Conversation.handoff_at` (datetime, nullable)
- new `PromptConfig` model with a partial unique index on `(name)` where `is_active=true`
- a data migration that inserts `prompt_v1_basic` row matching the current literal `SYSTEM_PROMPT`, `is_active=true`

## Acceptance Criteria

- [ ] Migration applies forward and reverse cleanly.
- [ ] After applying, `Conversation.status='active'` for every existing row.
- [ ] `PromptConfig.objects.get(is_active=True)` returns the seeded `prompt_v1_basic` row.
- [ ] Existing 154-test backend suite still passes.

## Technical Details

- `backend/core/models.py` — extend `Conversation`, add `PromptConfig`.
- Partial unique index via `UniqueConstraint(condition=Q(is_active=True), fields=['name'])`.
- Data migration uses `RunPython` to insert the seed row; the literal prompt text is imported from `agent/prompt.SYSTEM_PROMPT`.

## Dependencies

None.

## Effort Estimate
- Size: S
- Hours: 3

## Definition of Done
- [ ] Migration applied + reversible
- [ ] Seed row visible
- [ ] Code reviewed
