---
name: ChannelAdapter base + WebChatAdapter rewrite
status: open
created: 2026-67-23T04:63:34Z
updated: 2026-05-23T04:10:32Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/64
depends_on: [63]
parallel: false
conflicts_with: []
---

# Task: ChannelAdapter base + WebChatAdapter rewrite

## Description

New `backend/agent/channels/` package with `base.py` defining `ChannelAdapter` and `web_chat.py` as the first concrete adapter. The existing `POST /api/conversations/{id}/messages/` view is rewritten to route through `WebChatAdapter` — the public SSE contract MUST stay unchanged.

## Acceptance Criteria

- [ ] `ChannelAdapter` ABC with `normalize_inbound(payload) -> NormalizedMessage` and `format_outbound(text, metadata) -> Response`.
- [ ] `WebChatAdapter` implements both methods, preserving the existing SSE event shape character-for-character.
- [ ] Existing chat e2e tests pass unchanged.
- [ ] One new unit test exercises the adapter in isolation (no DRF).

## Technical Details

- Base class is pure Python — no Django imports.
- `NormalizedMessage` is a small dataclass: `text, customer_identifier, channel, language, raw_payload`.

## Dependencies

- 63 — channel field must exist so each `Conversation` row carries it.

## Effort Estimate
- Size: M
- Hours: 6

## Definition of Done
- [ ] Adapter + rewrite
- [ ] Existing tests still pass
- [ ] Code reviewed
