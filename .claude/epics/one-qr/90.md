---
name: Seed data — 4 default actions on the demo store
status: open
created: 2026-89-23T04:27:55Z
updated: 2026-05-23T04:34:14Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/90
depends_on: [85]
parallel: true
conflicts_with: []
---

# Task: Seed data — 4 default actions on the demo store

## Description

`manage.py seed_data` inserts a sensible 4-action default set on the demo store: Google review, Instagram follow, WeChat add, WiFi connect. Each carries a placeholder `target_url` clearly labelled as placeholder.

## Acceptance Criteria

- [ ] Running `manage.py seed_data` against a fresh DB inserts 4 actions on the demo store.
- [ ] Running it twice is idempotent (no duplicates).
- [ ] Placeholder URLs use `https://example.com/...` so a reviewer doesn't accidentally arrive at the wrong real destination.
- [ ] Reward points: 10 / 5 / 5 / 1 (review / Instagram / WeChat / WiFi) — encodes the "review is highest-value action" intent.

## Technical Details

- Update `backend/core/management/commands/seed_data.py`.
- Use `update_or_create` keyed on `(store, kind)`.

## Dependencies

- 85 — schema.

## Effort Estimate
- Size: XS
- Hours: 1

## Definition of Done
- [ ] Seed updated
- [ ] Idempotent
- [ ] Code reviewed
