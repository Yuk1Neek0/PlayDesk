---
name: Trace API — GET /api/admin/conversations/{id}/trace/
status: open
created: 2026-48-23T04:44:34Z
updated: 2026-05-23T04:09:51Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/46
depends_on: [44]
parallel: true
conflicts_with: []
---

# Task: Trace API — GET /api/admin/conversations/{id}/trace/

## Description

New staff-only DRF view returning the full ordered turn list for a conversation. Serializer includes the new trace fields plus the existing content / tool_call_data; cost is computed at serialization time from `model_name` + `tokens_*`.

## Acceptance Criteria

- [ ] `GET /api/admin/conversations/{id}/trace/` returns 200 with the full message list (paginated when >200 messages).
- [ ] Each message includes: `id, role, content, tool_call_data, latency_ms, tokens_in, tokens_out, model_name, prompt_version, retrieval_chunk_ids, estimated_cost_usd`.
- [ ] Non-staff requests return 403.
- [ ] Three tests: shape, auth gate, pagination cap.

## Technical Details

- `backend/api/admin_traces.py` — `ConversationTraceView(generics.RetrieveAPIView)` + `TraceMessageSerializer`.
- Reuses existing staff auth gate from admin endpoints.
- Cost helper imported from `agent/pricing.py` (task 49 owns the helper; this task initially stubs it to return 0.0 if pricing.py doesn't exist yet, then unblocks once 49 lands).

## Dependencies

- 44 — migration must land first.
- Coordinates with 49 (cost helper) — serializer field is `null` until 49 wires it up.

## Effort Estimate
- Size: S
- Hours: 3

## Definition of Done
- [ ] Endpoint + serializer in place
- [ ] Tests pass
- [ ] Code reviewed
