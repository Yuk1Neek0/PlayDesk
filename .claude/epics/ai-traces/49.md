---
name: Cost helper — agent/pricing.py + cost columns wired
status: open
created: 2026-48-23T04:44:34Z
updated: 2026-05-23T04:09:51Z
github: https://github.com/Yuk1Neek0/PlayDesk/issues/49
depends_on: [45, 46]
parallel: true
conflicts_with: []
---

# Task: Cost helper — agent/pricing.py + cost columns wired

## Description

A static `MODEL_PRICING` map keyed by `model_name`, plus a pure-function `estimate_cost(model_name, tokens_in, tokens_out) -> float`. Wire it into the trace serializer (`estimated_cost_usd`) and the list-page total.

## Acceptance Criteria

- [ ] `backend/agent/pricing.py` exports `estimate_cost` and `MODEL_PRICING`.
- [ ] Returns `0.0` for unknown model names (not None — so totals add cleanly).
- [ ] Trace serializer emits `estimated_cost_usd` per message; list endpoint emits per-conversation total.
- [ ] Unit tests cover three model names plus the unknown-model fallback.

## Technical Details

- `MODEL_PRICING` keys: `claude-opus-4-7`, `claude-sonnet-4-6`, `claude-haiku-4-5-20251001` — values in dollars per 1K tokens for input / output.
- Comment the pricing source and last-checked date.

## Dependencies

- 45 — needs captured `tokens_*` and `model_name`.
- 46 — extends the existing trace serializer.

## Effort Estimate
- Size: XS
- Hours: 2

## Definition of Done
- [ ] Helper + tests
- [ ] Serializer wired
- [ ] Code reviewed
