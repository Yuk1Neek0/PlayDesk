---
name: CI activation & project docs
status: open
created: 2026-05-21T19:06:21Z
updated: 2026-05-21T19:06:21Z
github: (will be set on sync)
depends_on: [1, 3]
parallel: false
conflicts_with: []
---

# Task: CI activation & project docs

## Description

Integration task: confirm the committed CI pipeline activates against the new scaffold and produces green `backend` and `frontend` jobs, then write the project-level documentation.

## Acceptance Criteria
- [ ] The `.github/workflows/ci.yml` `detect` job sees `backend/manage.py` and `frontend/package.json`; the `backend` and `frontend` jobs activate.
- [ ] Both jobs pass: ruff + migrations + pytest (backend); lint + typecheck + vitest + build (frontend).
- [ ] `ci-ok` aggregate gate is green.
- [ ] Root `README.md` documents the monorepo layout and the `docker compose up` quickstart.
- [ ] `CLAUDE.md` is updated from the CCPM-generated stub with PlayDesk-specific instructions (stack, layout, test commands).

## Technical Details
- Run jobs locally / inspect the CI run after the foundation branch lands.
- Fix any workflow path or script-name mismatches surfaced by the first real run.

## Dependencies
Task 001 (backend scaffold) and Task 003 (frontend scaffold) must exist for CI jobs to activate.

## Effort Estimate
- Size: S
- Hours: 2

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
